{
    "search": {{ point.json|safe }},
    "neighborhood": {% if hood %}{
        "name":"{{ hood.name }}",
        "slug":"{{ hood.name|slugify }}",
        "centroid": {{ hood.simple_shape.centroid.json|safe }}
    }{% else %}null{% endif %},
    "region": {% if region %}{
        "name":"{{ region.name }}",
        "slug":"{{ region.name|slugify }}"
    }{% else %}null{% endif %}
}
